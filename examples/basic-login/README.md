# Basic Login Example

This example uses the mock adapter and a recorded action sequence to show the first runtime loop:

1. navigate;
2. fill email;
3. fill password;
4. click submit;
5. extract text.

It is intentionally local and deterministic so package tests can run without opening a browser.
